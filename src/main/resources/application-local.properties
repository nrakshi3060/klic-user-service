# Local profile configuration
# Liquibase is disabled by default in application.properties
# This profile can be used for local development against a pre-initialized database
spring.jpa.hibernate.ddl-auto=none
